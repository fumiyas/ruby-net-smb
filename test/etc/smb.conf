[global]
private dir = %$(TEST_SMBD_VAR_DIR)
lock directory = %$(TEST_SMBD_VAR_DIR)
state directory = %$(TEST_SMBD_VAR_DIR)
cache directory = %$(TEST_SMBD_VAR_DIR)
pid directory = %$(TEST_SMBD_VAR_DIR)
ncalrpc dir = %$(TEST_SMBD_VAR_DIR)

[share]
path = %$(TEST_SMBD_SHARE_DIR)
writeable = yes

[共有]
copy = share

[共有フォルダー]
copy = share

[share2]
copy = share
writeable = no

